package volumes.solidos;

public class Esfera {

	protected double calcEsfera(double V, double r) {
		
		return V = (4/3) * Math.PI * Math.pow(r, 3);
	
		/* LEGENDAS
		 * V = Volume
		 * r = Raio
		 */
	}
}
