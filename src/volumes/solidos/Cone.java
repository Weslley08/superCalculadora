package volumes.solidos;

public class Cone {

	protected double calcCone(double V, double r, double h) {
		
		return V = (1/3) * Math.PI * Math.pow(r, 2) * h;
	
		/* LEGENDAS
		 * V = Volume
		 * r = Raio
		 * h = Altura
		 */
	}
}