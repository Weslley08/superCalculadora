package volumes.solidos;

public class Cilindro {

	protected double calcCilindro(double V, double r, double h) {
		
		return V = Math.PI * Math.pow(r, 2) * h;
	
		/* LEGENDAS
		 * V = Volume
		 * r = Raio
		 * h = Altura
		 */
	}
}