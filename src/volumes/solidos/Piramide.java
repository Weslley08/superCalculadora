package volumes.solidos;

public class Piramide {

	protected double calcPiramide(double V, double Ab, double h) {
		
		return V = (1/3) * Ab * h;
	
		/* LEGENDAS
		 * V = Volume
		 * r = Raio
		 * Ab = Área da base
		 */
	}
}